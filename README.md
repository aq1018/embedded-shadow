# embedded-shadow (DEPRECATED)

This crate has been renamed and moved to [`embedded-registry`](https://github.com/aq1018/embedded-registry).

Please update your dependencies:

```toml
[dependencies]
embedded-registry = "0.2.0"
```

All functionality remains the same - only the naming has changed from "shadow" to "registry".
